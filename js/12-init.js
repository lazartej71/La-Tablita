/* ====================================================================
   12-init.js
   Arranque de la aplicación: se ejecuta al final, cuando ya están
   cargados los datos y todas las funciones de renderizado.
   ==================================================================== */

/* ======================= INIT ======================= */
aplicarConfigUI();
renderClienteSelect();
renderMenuSelect();
renderItemsPedido();
renderMenu();
renderClientes();
renderComandas();
renderCaja();
